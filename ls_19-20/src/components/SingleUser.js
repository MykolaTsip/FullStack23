
import {withRouter} from 'react-router'

 const SingleUser = (prop) => {

    console.log(prop);
    return (
    <div>
        Heeloo user
    </div>
    )
}

// export default withRouter(SingleUser)
export default SingleUser