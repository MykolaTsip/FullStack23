* Для кожного наступного пункту створити окрему сторінку в якій буде доступний перехід на домашню сторніку,
  посилання показати в Navbar.
* Намагайтесь виводити дані в константи (по прикладу лекції).

1. Створити кастомний хук useCount який буде зберігати стан для count, та хук useTime який буде при useEffect виводити
   в консоль: 'i was called at: <data>'

2. Створити контрольовану форму, (аналогічно попередньому дз, використовувати через хук useReducer). Написати свою валідацію.

3. Створити не контрольовану форму, (аналогічно попередньому дз, використовувати через хук useReducer)

4* Створити форму використовуючи бібліотеки formik та yup.